<Window x:Class="UI.AutomationTests.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        ResizeMode="CanResize"
        Title="UI Automation Test Window" Height="350" Width="525">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="24,24,24,0"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Margin" Value="24,24,24,0"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="24,24,24,0"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <ListView Grid.Row="0"
                  Grid.Column="0"/>

        <StackPanel Grid.Row="0"
                    Grid.Column="1">
            <TextBox Name="txtA" />
            <TextBox Name="txtB" />
            <TextBox Name="txtC" />
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Grid.Column="0">
            <RadioButton GroupName="RadioButtonsGroup"/>
            <RadioButton GroupName="RadioButtonsGroup"/>
            <RadioButton GroupName="RadioButtonsGroup"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Grid.Column="1">
            <Button Name="btnClick"
                    Content="Click" Click="btnClick_Click"/>
            <Button Name="btnCancel"
                    Content="Cancel"/>
        </StackPanel>
    </Grid>
</Window>
